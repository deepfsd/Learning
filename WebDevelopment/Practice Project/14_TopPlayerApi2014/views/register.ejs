<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register Form</title>
    <link rel="stylesheet" href="/static/register.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>

<body>
    <div class="registerPage">
        <a class="button-confirm token" href="/tokenPage"><span>Token →</span></a>
        <form class="form" action="/register" method="post">
            <div class="title">Welcome,<br><span>register to continue</span></div>
            <input class="input" name="username" placeholder="Username" type="text">
            <input class="input" name="password" placeholder="Password" type="password">
            <div class="login-with">
                <% if(locals.successMessage){ %>
                    <span style="color: green;font-size: 10px;margin-left: 10px;font-weight: 700;">
                        <%= successMessage %>
                    </span>
                    <% }else if(locals.invalidMessage){ %>
                        <span style="color: red;font-size: 10px;margin-left: 10px;font-weight: 700;">
                            <%= locals.invalidMessage %> <br>
                                <%= locals.userMessage %> <br>
                                    <%= locals.passMessage %>
                        </span>
                        <% } %>
            </div>
            <button class="register-button-confirm" type="submit">Register</button>
        </form>
    </div>
</body>

</html>